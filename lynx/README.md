# Lynx

Make sure the `lynx` script comes before normal the actual program in `bin` or similar.

Scripts related to lynx:

* lynx
* urlencode
* duck
* marginalia
* wiki
* wiby
* startpage
* google

Recommended for better experience:

* `?` aliased as `duck`
* `??` aliased as `marginalia`
